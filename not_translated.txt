%%[if empty(@flightdetails) then]%%  >>>>  %%[if empty(@flightdetails) then]%%

%%[if empty(@guestdetails) then]%%  >>>>  %%[if empty(@guestdetails) then]%%

%%[if empty(@postaladdress) then]%%  >>>>  %%[if empty(@postaladdress) then]%%

%%[if empty(@postaladdress) then]%%  >>>>  %%[if empty(@postaladdress) then]%%

